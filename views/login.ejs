<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Elreem Bag Store</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="/css/style.css" rel="stylesheet">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Buffy:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Aref+Ruqaa:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Amiri:wght@400;700&display=swap" rel="stylesheet">

    <!-- Responsive Login Box Styles -->
    <style>
        .responsive-login-box {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            padding: 3rem;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            border: 2px solid #D4A574;
            width: 90%;
            margin: 0 auto;
        }

        /* Desktop and Large Tablets */
        @media (min-width: 768px) {
            .responsive-login-box {
                width: 100%;
                padding: 3rem;
            }
        }

        /* Small Tablets */
        @media (max-width: 767px) and (min-width: 481px) {
            .responsive-login-box {
                width: 95%;
                padding: 2rem;
            }
        }

        /* Mobile Phones */
        @media (max-width: 480px) {
            .responsive-login-box {
                width: 95%;
                padding: 1.5rem;
                border-radius: 15px;
            }

            .responsive-login-box h2 {
                font-size: 1.5rem !important;
            }

            .responsive-login-box .btn-lg {
                padding: 0.75rem !important;
                font-size: 1rem !important;
            }
        }

        /* Extra Small Phones */
        @media (max-width: 360px) {
            .responsive-login-box {
                padding: 1.25rem;
                border-radius: 12px;
            }
        }
    </style>
</head>
<body class="auth-page">
    <div class="container-fluid h-100">
        <div class="row h-100">
            <!-- Login Form - Full Width -->
            <div class="col-12 d-flex align-items-center justify-content-center min-vh-100 px-3">
                <div class="w-100" style="max-width: 600px;">
                    <div class="auth-form-container responsive-login-box">
                        <!-- Language Toggle -->
                        <div class="text-end mb-4">
                            <button class="btn btn-outline-primary btn-sm" id="languageToggle">
                                <i class="fas fa-language me-1"></i>
                                <span id="currentLang">EN</span>
                            </button>
                        </div>
                        
                        <div class="text-center mb-3">
                            <img src="/images/logo.png" alt="Elreem Logo" height="180" class="mb-3">
                            <h3 class="fw-bold" data-en="Welcome Back" data-ar="مرحباً بعودتك">Welcome Back</h3>
                            <p class="text-muted" data-en="Sign in to your account" data-ar="سجل دخولك إلى حسابك">Sign in to your account</p>
                        </div>

                        <!-- Flash Messages -->
                        <% if (error && error.length > 0) { %>
                            <div class="alert alert-danger" role="alert">
                                <% error.forEach(function(message) { %>
                                    <%= message %>
                                <% }); %>
                            </div>
                        <% } %>
                        
                        <% if (success && success.length > 0) { %>
                            <div class="alert alert-success" role="alert">
                                <% success.forEach(function(message) { %>
                                    <%= message %>
                                <% }); %>
                            </div>
                        <% } %>

                        <!-- Login Form -->
                        <form action="/login" method="POST" class="needs-validation" novalidate>
                            <div class="mb-3">
                                <label for="email" class="form-label" data-en="Email Address" data-ar="البريد الإلكتروني">Email Address</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                                    <input type="email" class="form-control" id="email" name="email" required>
                                    <div class="invalid-feedback" data-en="Please provide a valid email." data-ar="يرجى إدخال بريد إلكتروني صحيح.">
                                        Please provide a valid email.
                                    </div>
                                </div>
                            </div>

                            <div class="mb-3">
                                <label for="password" class="form-label" data-en="Password" data-ar="كلمة المرور">Password</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-lock"></i></span>
                                    <input type="password" class="form-control" id="password" name="password" required>
                                    <button class="btn btn-outline-secondary" type="button" id="togglePassword">
                                        <i class="fas fa-eye"></i>
                                    </button>
                                    <div class="invalid-feedback" data-en="Please provide a password." data-ar="يرجى إدخال كلمة المرور.">
                                        Please provide a password.
                                    </div>
                                </div>
                            </div>

                            <button type="submit" class="btn btn-primary w-100 mb-3">
                                <i class="fas fa-sign-in-alt me-2"></i>
                                <span data-en="Sign In" data-ar="تسجيل الدخول">Sign In</span>
                            </button>
                        </form>

                        <!-- Google Login -->
                        <div class="text-center mb-3">
                            <span class="text-muted" data-en="or" data-ar="أو">or</span>
                        </div>
                        
                        <a href="/auth/google" class="btn btn-outline-danger w-100 mb-4">
                            <i class="fab fa-google me-2"></i>
                            <span data-en="Continue with Google" data-ar="المتابعة مع جوجل">Continue with Google</span>
                        </a>

                        <!-- Sign up link -->
                        <div class="text-center">
                            <p class="mb-0">
                                <span data-en="Don't have an account?" data-ar="ليس لديك حساب؟">Don't have an account?</span>
                                <a href="/signup" class="text-decoration-none" data-en="Sign up" data-ar="إنشاء حساب">Sign up</a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Custom JS -->
    <script src="/js/main.js"></script>
    <script src="/js/auth.js"></script>
</body>
</html>
